import React from 'react';
import './LoadingScreen.css';

function LoadingScreen() {

    return (
        <div className="loadingScreen">
            <span className="icon rotating">course</span>
        </div>
    );
}

export default LoadingScreen;